import 'package:flutter/material.dart';

class Dimens {
  static final loadingBorderRadius = BorderRadius.circular(50);
  static final borderRadius = BorderRadius.circular(15);

  static const defaultWidthSpace = const SizedBox(width: 16);
  static const defaultHeightSpace = const SizedBox(height: 16);

  static const smallWidthSpace = const SizedBox(width: 8);
  static const smallHeightSpace = const SizedBox(height: 8);

  static const largerWidthSpace = const SizedBox(width: 32);
  static const largerHeightSpace = const SizedBox(height: 32);

  static const zero = 0.0;

  static const defaultPadding = 8.0;
  static const smallPadding = 4.0;
  static const largePadding = 16.0;

  static const iconButton = 36.0;
  static const normalButton = 88.0;
  static const loadingSize = 48.0;
}
